@model DisqussTopics.Models.Topic

@{
    ViewData["Title"] = "Delete Topic";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this Topic?</h3>
<div>
    <h4>Test</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.About)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.About)
        </dd>
    </dl>

    <form asp-action="Delete" asp-route-slug="@Model.Slug">
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-action="Detail" asp-route-slug="@Model.Slug">Back to Topic</a>
    </form>
</div>